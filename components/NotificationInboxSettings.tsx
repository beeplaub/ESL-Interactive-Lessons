"use client";

import Link from "next/link";
import { Bell, CheckCheck, Mail, MailOpen, Save, Trash2 } from "lucide-react";
import { useState, useTransition } from "react";
import { deleteNotification, markAllNotificationsRead, markNotificationRead, markNotificationUnread, saveNotificationPreferences } from "@/app/notifications/actions";
import { PushNotificationControl } from "@/components/PushNotificationControl";

type Notice = { id: string; title: string; detail: string | null; href: string | null; action_label: string | null; tone: "purple" | "orange" | "green" | "blue"; category: string; read_at: string | null; created_at: string };
type Preference = { category: string; in_app_enabled: boolean; push_enabled: boolean; email_enabled: boolean };
const categories = ["LEARNING", "ASSIGNMENTS", "LIVE_CLASSES", "ACHIEVEMENTS", "ANNOUNCEMENT", "ACCOUNT", "SUPPORT"];
const toneClass = { purple: "bg-[var(--br-brand)]", orange: "bg-[var(--br-action-strong)]", green: "bg-[var(--br-success)]", blue: "bg-[var(--br-info)]" };

export function NotificationInboxSettings({ notices, preferences, pushEnabled }: { notices: Notice[]; preferences: Preference[]; pushEnabled: boolean }) {
  const [pending, startTransition] = useTransition();
  const [status, setStatus] = useState("");
  const [localNotices, setLocalNotices] = useState(notices);
  const preferenceFor = (category: string) => preferences.find((item) => item.category === category);
  const unread = localNotices.filter((item) => !item.read_at).length;
  return <div className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_350px]">
    <section className="rounded-[22px] border border-[var(--br-border)] bg-surface p-4 shadow-[var(--br-shadow)] sm:p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-[10px] font-black uppercase tracking-[0.15em] text-[var(--br-brand)]">Your inbox</p><h1 className="mt-1 text-2xl font-black text-ink">Notifications</h1><p className="mt-1 text-sm text-[var(--br-text-muted)]">Important BrenUp updates, in one calm place.</p></div>{unread ? <button type="button" disabled={pending} onClick={() => startTransition(async () => { await markAllNotificationsRead(); setLocalNotices((current) => current.map((item) => ({ ...item, read_at: item.read_at || new Date().toISOString() }))); })} className="inline-flex items-center gap-2 rounded-xl border border-[var(--br-border)] px-3 py-2 text-xs font-extrabold text-[var(--br-brand)]"><CheckCheck className="size-4" /> Mark all read</button> : null}</div><div className="mt-5 divide-y divide-[var(--br-border)]">{localNotices.map((notice) => <article key={notice.id} className={`flex gap-3 py-4 ${notice.read_at ? "opacity-65" : ""}`}><span className={`mt-1.5 size-2.5 shrink-0 rounded-full ${toneClass[notice.tone]}`} /><div className="min-w-0 flex-1"><div className="flex flex-wrap items-start justify-between gap-2"><div><p className={`text-sm ${notice.read_at ? "font-bold text-[var(--br-text-muted)]" : "font-extrabold text-ink"}`}>{notice.title}</p><p className="mt-1 text-xs font-bold uppercase tracking-[0.1em] text-[var(--br-text-muted)]">{notice.category.replaceAll("_", " ")} · {new Date(notice.created_at).toLocaleString()}</p></div></div>{notice.detail ? <p className="mt-2 text-sm leading-6 text-[var(--br-text-muted)]">{notice.detail}</p> : null}{notice.href ? <Link href={notice.href} onClick={() => { if (!notice.read_at) { void markNotificationRead(notice.id); setLocalNotices((current) => current.map((item) => item.id === notice.id ? { ...item, read_at: new Date().toISOString() } : item)); } }} className="mt-3 inline-flex text-xs font-extrabold text-[var(--br-brand)] hover:underline">{notice.action_label || "Open"}</Link> : null}</div><div className="flex shrink-0 flex-col gap-1"><button type="button" title={notice.read_at ? "Mark unread" : "Mark read"} onClick={() => { const wasRead = Boolean(notice.read_at); setLocalNotices((current) => current.map((item) => item.id === notice.id ? { ...item, read_at: wasRead ? null : new Date().toISOString() } : item)); void (wasRead ? markNotificationUnread(notice.id) : markNotificationRead(notice.id)); }} className="grid size-8 place-items-center rounded-lg text-[var(--br-text-muted)] hover:bg-[var(--br-surface-muted)]">{notice.read_at ? <Mail className="size-4" /> : <MailOpen className="size-4" />}</button><button type="button" title="Archive" onClick={() => { setLocalNotices((current) => current.filter((item) => item.id !== notice.id)); void deleteNotification(notice.id); }} className="grid size-8 place-items-center rounded-lg text-[var(--br-text-muted)] hover:bg-[var(--br-surface-muted)] hover:text-[var(--br-danger)]"><Trash2 className="size-4" /></button></div></article>)}{!localNotices.length ? <div className="py-14 text-center"><Bell className="mx-auto size-7 text-[var(--br-brand)]" /><p className="mt-3 text-sm font-bold text-ink">You are all caught up.</p><p className="mt-1 text-xs text-[var(--br-text-muted)]">New updates will appear here when they matter.</p></div> : null}</div></section>
    <aside className="space-y-4"><section className="rounded-[22px] border border-[var(--br-border)] bg-surface p-4 shadow-[var(--br-shadow)]"><h2 className="text-base font-black text-ink">Delivery preferences</h2><p className="mt-1 text-xs leading-5 text-[var(--br-text-muted)]">Choose how optional updates reach you. Account and security notices stay protected.</p><form onSubmit={(event) => { event.preventDefault(); const form = event.currentTarget; startTransition(async () => { const result = await saveNotificationPreferences(new FormData(form)); setStatus(result.success ? "Preferences saved." : result.error || "Could not save preferences."); }); }} className="mt-4 space-y-3">{categories.map((category) => { const value = preferenceFor(category); const essential = category === "ACCOUNT" || category === "SUPPORT"; return <div key={category} className="rounded-xl bg-[var(--br-surface-muted)] p-3"><p className="text-xs font-extrabold text-ink">{category.replaceAll("_", " ")}</p><div className="mt-2 flex flex-wrap gap-2 text-[11px] font-bold text-[var(--br-text-muted)]"><label><input type="checkbox" name={`${category}:IN_APP`} defaultChecked={essential || value?.in_app_enabled !== false} disabled={essential} /> In-app</label><label><input type="checkbox" name={`${category}:PUSH`} defaultChecked={value?.push_enabled !== false} /> Push</label><label><input type="checkbox" name={`${category}:EMAIL`} defaultChecked={value?.email_enabled === true} /> Email</label></div></div>; })}<button disabled={pending} className="inline-flex w-full items-center justify-center gap-2 rounded-xl bg-[var(--br-brand)] px-3 py-2.5 text-xs font-extrabold text-on-dark disabled:opacity-60"><Save className="size-4" /> Save preferences</button>{status ? <p className="text-center text-xs font-semibold text-[var(--br-text-muted)]">{status}</p> : null}</form></section><PushNotificationControl initialEnabled={pushEnabled} /></aside>
  </div>;
}
