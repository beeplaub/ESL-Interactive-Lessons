import { grantPracticeLibraryAccess, revokePracticeLibraryAccess } from "@/app/activities/actions";

type Grant = { id: string; reason: string | null; starts_at: string; ends_at: string | null; revoked_at: string | null };

export function PracticeAccessGrants({ userId, grants }: { userId: string; grants: Grant[] }) {
  return <section className="rounded-xl border border-[var(--br-border)] bg-surface p-5 shadow-sm"><h2 className="text-lg font-semibold">Premium Practice access</h2><p className="mt-1 text-sm text-[var(--br-text-muted)]">Grant or revoke direct library access for this learner.</p><div className="mt-3 space-y-2">{grants.map((grant) => <div key={grant.id} className="flex flex-wrap items-center justify-between gap-3 rounded-lg bg-black/[0.03] px-3 py-2 text-sm"><span>{grant.revoked_at ? "Revoked" : grant.ends_at ? `Until ${new Date(grant.ends_at).toLocaleDateString()}` : "Permanent"}{grant.reason ? ` · ${grant.reason}` : ""}</span>{!grant.revoked_at ? <form action={revokePracticeLibraryAccess.bind(null, userId, grant.id)}><button className="text-xs font-bold text-rose-700">Revoke</button></form> : null}</div>)}{!grants.length ? <p className="text-sm text-[var(--br-text-muted)]">No direct grants.</p> : null}</div><form className="mt-4 space-y-3 border-t border-[var(--br-border)] pt-4"><label className="block text-xs font-bold">Reason<input name="reason" placeholder="Scholarship, promotion, support…" className="mt-1 w-full rounded-lg border border-[var(--br-border)] px-3 py-2 text-sm font-normal" /></label><div className="flex flex-wrap gap-2">{([["1", "1 month"], ["6", "6 months"], ["12", "12 months"], ["PERMANENT", "Permanent"]] as const).map(([term, label]) => <button key={term} formAction={grantPracticeLibraryAccess.bind(null, userId, term)} className="rounded-lg border border-[var(--br-border)] px-3 py-2 text-xs font-bold">Grant {label}</button>)}</div></form></section>;
}
