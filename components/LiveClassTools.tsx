"use client";

import { Hand, MessageCircle, Send, SlidersHorizontal, Vote } from "lucide-react";
import { useCallback, useEffect, useRef, useState } from "react";
import { createClient } from "@/lib/supabase/client";

type Data = { messages: Array<{ id: string; sender_id: string; sender_name: string; channel: string; body: string; created_at: string }>; hands: Array<{ id: string; user_id: string; user_name: string; kind: string }>; polls: Array<{ id: string; question: string; poll_type: string; options: unknown; status: string }>; ownAnswers: Array<{ poll_id: string; answer: unknown }>; teacher: boolean };

export function LiveClassTools({ sessionId, teacher: initialTeacher = false }: { sessionId: string; teacher?: boolean }) {
  const [data, setData] = useState<Data | null>(null); const [message, setMessage] = useState(""); const [channel, setChannel] = useState("EVERYONE"); const [error, setError] = useState<string | null>(null); const [pollQuestion, setPollQuestion] = useState(""); const [pollType, setPollType] = useState("MCQ"); const [pollOptions, setPollOptions] = useState("Yes\nNo"); const refreshing = useRef(false);
  const teacher = data?.teacher ?? initialTeacher;
  const refresh = useCallback(async () => { if (refreshing.current) return; refreshing.current = true; try { const r = await fetch(`/api/live/${sessionId}/interactions`, { cache: "no-store" }); if (r.ok) setData(await r.json()); } finally { refreshing.current = false; } }, [sessionId]);
  async function act(payload: Record<string, unknown>) { setError(null); const r = await fetch(`/api/live/${sessionId}/interactions`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(payload) }); const body = await r.json().catch(() => ({})); if (!r.ok) setError(body.error || "Could not update the class."); else { await refresh(); } }
  useEffect(() => { void refresh(); const s = createClient(); const c = s.channel(`brenup-live-tools:${sessionId}`).on("broadcast", { event: "refresh" }, () => void refresh()).subscribe(); const interval = window.setInterval(refresh, 3500); return () => { clearInterval(interval); void s.removeChannel(c); }; }, [refresh, sessionId]);
  function announce(payload: Record<string, unknown>) { void act(payload).then(() => { const s = createClient(); void s.channel(`brenup-live-tools:${sessionId}`).send({ type: "broadcast", event: "refresh", payload: {} }); }); }
  const activePoll = data?.polls.find((poll) => poll.status === "OPEN") ?? null;
  return <aside className="space-y-3"><section className="rounded-xl border border-black/10 bg-white p-3 shadow-sm"><div className="flex items-center justify-between gap-2"><div className="flex items-center gap-1.5"><MessageCircle size={16} className="text-violetglow" /><h2 className="text-sm font-extrabold">Class chat</h2></div><select value={channel} onChange={(e) => setChannel(e.target.value)} className="rounded-md border border-black/10 bg-white px-1.5 py-1 text-[10px] font-bold text-black/55"><option value="EVERYONE">Everyone</option><option value="TEACHER">Teacher</option></select></div><div className="mt-3 max-h-52 space-y-2 overflow-y-auto pr-1">{data?.messages.length ? data.messages.map((item) => <div key={item.id} className="rounded-lg bg-slate-50 p-2"><p className="text-[10px] font-extrabold text-violetglow">{item.sender_name} {item.channel === "TEACHER" ? "· private" : ""}</p><p className="mt-0.5 text-xs text-black/70">{item.body}</p></div>) : <p className="py-4 text-center text-xs text-black/45">Class chat will appear here.</p>}</div><div className="mt-3 flex gap-1"><input value={message} onChange={(e) => setMessage(e.target.value)} onKeyDown={(e) => { if (e.key === "Enter") { e.preventDefault(); if (message.trim()) { announce({ action: "message", body: message, channel }); setMessage(""); } } }} placeholder="Write a message..." className="min-w-0 flex-1 rounded-md border border-black/10 px-2 py-1.5 text-xs" /><button type="button" onClick={() => { if (message.trim()) { announce({ action: "message", body: message, channel }); setMessage(""); } }} className="grid size-8 place-items-center rounded-md bg-violetglow text-white" aria-label="Send message"><Send size={14} /></button></div></section><section className="rounded-xl border border-black/10 bg-white p-3 shadow-sm"><div className="flex items-center justify-between gap-2"><div className="flex items-center gap-1.5"><Hand size={16} className="text-violetglow" /><h2 className="text-sm font-extrabold">Class signals</h2></div><span className="text-[10px] font-bold text-black/45">{data?.hands.length ?? 0} active</span></div>{teacher ? <div className="mt-2 space-y-1">{data?.hands.length ? data.hands.map((hand) => <button key={hand.id} type="button" onClick={() => announce({ action: "resolveHand", handId: hand.id })} className="flex w-full items-center justify-between rounded-md bg-amber-50 px-2 py-1.5 text-left text-xs"><span>{hand.kind === "HELP" ? "Needs help" : "Hand raised"} · <b>{hand.user_name}</b></span><span className="font-bold text-violetglow">Resolve</span></button>) : <p className="py-2 text-xs text-black/45">No raised hands.</p>}</div> : <div className="mt-3 flex gap-2"><button type="button" onClick={() => announce({ action: "hand", kind: "HAND" })} className="flex-1 rounded-md bg-[#EEEAFB] px-2 py-2 text-xs font-bold text-violetglow">Raise hand</button><button type="button" onClick={() => announce({ action: "hand", kind: "HELP" })} className="flex-1 rounded-md bg-amber-50 px-2 py-2 text-xs font-bold text-amber-700">Need help</button></div>}</section>{teacher ? <section className="rounded-xl border border-black/10 bg-white p-3 shadow-sm"><div className="flex items-center gap-1.5"><Vote size={16} className="text-violetglow" /><h2 className="text-sm font-extrabold">Quick poll</h2></div><input value={pollQuestion} onChange={(e) => setPollQuestion(e.target.value)} placeholder="Ask the class..." className="mt-3 w-full rounded-md border border-black/10 px-2 py-1.5 text-xs" /><div className="mt-2 flex gap-2"><select value={pollType} onChange={(e) => setPollType(e.target.value)} className="min-w-0 flex-1 rounded-md border border-black/10 px-2 py-1.5 text-xs"><option value="MCQ">Multiple choice</option><option value="TRUE_FALSE">True / False</option><option value="WORD_CLOUD">Word cloud</option><option value="EMOJI">Emoji</option><option value="RATING">Rating</option></select><button type="button" onClick={() => { if (pollQuestion.trim()) { announce({ action: "createPoll", question: pollQuestion, pollType, options: pollOptions }); setPollQuestion(""); } }} className="rounded-md bg-violetglow px-2.5 text-xs font-bold text-white">Open</button></div>{pollType === "MCQ" ? <textarea value={pollOptions} onChange={(e) => setPollOptions(e.target.value)} rows={2} placeholder="One option per line" className="mt-2 w-full rounded-md border border-black/10 px-2 py-1.5 text-xs" /> : null}</section> : null}{activePoll ? <section className="rounded-xl border border-violetglow/20 bg-violetglow/5 p-3"><p className="text-[10px] font-bold uppercase tracking-wide text-violetglow">Live poll</p><h2 className="mt-1 text-sm font-extrabold">{activePoll.question}</h2><div className="mt-3 grid gap-1.5">{activePoll.poll_type === "MCQ" ? (Array.isArray(activePoll.options) ? activePoll.options : []).map((option) => <button key={String(option)} type="button" onClick={() => announce({ action: "answerPoll", pollId: activePoll.id, answer: option })} className="rounded-md border border-violetglow/20 bg-white px-2 py-2 text-left text-xs font-semibold hover:bg-[#EEEAFB]">{String(option)}</button>) : <input onKeyDown={(e) => { if (e.key === "Enter") announce({ action: "answerPoll", pollId: activePoll.id, answer: (e.target as HTMLInputElement).value }); }} placeholder="Your answer..." className="rounded-md border border-violetglow/20 bg-white px-2 py-2 text-xs" />}</div></section> : null}{error ? <p className="rounded-md bg-red-50 p-2 text-xs font-semibold text-red-600">{error}</p> : null}</aside>;
}
