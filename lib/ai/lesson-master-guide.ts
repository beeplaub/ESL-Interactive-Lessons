/** Protected operational rules distilled from the BrenUp lesson creation guide. */
export const LESSON_MASTER_GUIDE = `BRENUP LESSON CREATION MASTER GUIDE (AUTHORITATIVE)
Hierarchy: course -> course section -> course item -> lesson -> ordered slides -> content blocks and at most one activity per slide. Questions live inside activity JSON. Build content before OBE mappings.
Before creating or changing content: identify the target course and section, inspect neighboring course_items and sibling lessons, reuse an existing draft shell, and stop for approval if the lesson already contains slides or outcomes. Never create a duplicate lesson. Read live block/activity examples and current schemas before using an unfamiliar type.
Pedagogy: write five measurable learner-facing outcomes covering vocabulary, grammar or functional language, receptive comprehension, and productive use. Build a coherent narrative and CEFR-appropriate sequence. Default A1-A2 lessons are usually 19-24 slides, but explicit user requirements control. Never invent extra slides. Do not repeat an activity type consecutively; use varied practice. Activities normally need at least three questions; matching needs five or more pairs.
Media: images must be distinct, real, direct, landscape URLs; never reuse an image. A slide with AUDIO or VIDEO must have framing TEXT immediately before the media and must not contain an IMAGE block. Never leave a non-media activity slide empty. Never duplicate a slide title with a HEADING block. Audio/video is supplied by the owner; never invent URLs or rewrite supplied questions.
Content: reading passages are original and level-appropriate. Use exact renderer-compatible JSON shapes. A slide title is already displayed by the player. Validate every answer against its options/items. Mark open-ended production activities needs_review=true; objective activities can be false. Do not silently replace, delete, or overwrite existing content.
Workflow: present a synopsis and slide plan before a substantial build; wait for approval unless the user explicitly authorizes building. Save only as DRAFT. Never publish, alter permissions, RLS, unrelated records, or learner data. Report uncertainty and source conflicts instead of guessing.`;
